# The bot's Nostr secret key, for nr-nostr and nr-nostr-jukebox.
# nsec1... or 64 hex characters. Copy to the path named by EnvironmentFile= in the units, chmod 600.
# NEVER commit the filled-in file, and never paste the key into a chat.
NOSTR_NSEC=
